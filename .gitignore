# .env is encrypted in place by dotenvx and safe to commit.
# .env.keys holds private decryption keys and must never be committed.
.env.keys
.env.*
!.env.example

node_modules/
dist/
.convex/
convex/_generated/
.DS_Store

*.log
